window.JST = {};

/* page */
window.JST['page'] = _.template(
    "<div class='contact'><%= name %> ..."
);

window.JST['page/header'] = _.template(
    "<div class='contact'><%= name %> ..."
);

window.JST['page/footer'] = _.template(
    "<div class='contact'><%= name %> ..."
);


/* report */
window.JST['report/list'] = _.template(
    "<div class='contact'><%= name %> ..."
);


window.JST['report/list'] = _.template(
    "<div class='contact'><%= name %> ..."
);

window.JST['report/listItem'] = _.template(
    "<form method='post'><input type..."
);